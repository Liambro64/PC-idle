using UnityEngine;

public class UpgradeWindow : Window
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // This should make the view inside the window, called in update
    public override void makeView()
    {

    }
    public override void makeFixedView()
    {

    }
}
